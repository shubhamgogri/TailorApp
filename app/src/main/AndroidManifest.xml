<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tailorapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TailorApp">
        <activity android:name=".Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Order_Details" />
        <activity android:name=".OrderList_tailor" />
        <activity android:name=".Home_page" />
        <activity android:name=".newproduct.AddNewProduct" />
        <activity android:name=".nav.cart.Payment" />
        <activity android:name=".nav.cart.NewAddress" />
        <activity android:name=".nav.cart.AddressContinue" />
        <activity android:name=".nav.cart.Checkout" />
        <activity
            android:name=".nav.cart.CartActivity"
            android:label="Cart" />
        <activity
            android:name=".nav.marketplace.ProductDetails"
            android:label="@string/title_activity_product_details" />
        <activity android:name=".registration.Form" />
        <activity
            android:name=".nav.Myshopmain"
            android:theme="@style/Theme.TailorApp.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".login.Forgotpassword" />
        <activity android:name=".registration.Otp_Registration" />
        <activity android:name=".registration.Register" />
        <activity android:name=".login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="@string/fb_login_protocol_scheme"
                    tools:ignore="AppLinkUrlError" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>